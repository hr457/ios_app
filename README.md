# SAFL Demo Project

Demo login:
- Mobile: 9999999999
- Password: 123456

Run:
```bash
flutter pub get
flutter run
```

If you already have a Flutter project, copy `lib/` and `pubspec.yaml` dependencies into your project.
